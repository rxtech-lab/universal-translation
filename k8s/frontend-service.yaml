apiVersion: v1
kind: Service
metadata:
  name: ut-frontend-service
  namespace: universal-translation
  labels:
    app: ut-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: ut-frontend
